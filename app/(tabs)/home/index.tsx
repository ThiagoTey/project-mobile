import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView>
      <Text>Bem vindo!</Text>
    </SafeAreaView>
  );
};

export default Home;
